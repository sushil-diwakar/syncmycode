import Contact from '../../src/components/Contact';

export const metadata = {
    title: 'Contact',
    description: 'Get in touch with the SyncMyCode team. Have questions or feedback? Drop us a message.',
};

export default function ContactPage() {
    return <Contact />;
}

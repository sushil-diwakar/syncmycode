import Login from '../../src/components/Login';

export const metadata = {
    title: 'Sign In',
    description: 'Sign in to your SyncMyCode account to access your collaborative workspace and saved snippets.',
};

export default function LoginPage() {
    return <Login />;
}
